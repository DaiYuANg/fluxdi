# FluxDI

FluxDI is a type-safe dependency injection container for Rust, focused on module-based composition, sync/async provider support, and lifecycle orchestration.

Primary docs:

- Crate README: `fluxdi/README.md`
- mdBook: `docs/`

Local docs commands:

```bash
mdbook build docs
mdbook serve docs --open
```
